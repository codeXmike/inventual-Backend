import Counter from '../models/Counter.js';

export const getNextBusinessId = async () => {
  // const counter = await Counter.findOneAndUpdate(
  //   { key: 'business' },
  //   { $inc: { seq: 1 } },
  //   { new: true, upsert: true }
  // );




  return customId;
};
